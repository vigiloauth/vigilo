package constants

// ID Prefix Constants define prefixes used for generating unique identifiers
// for various entities in the application.
const (
	AuditEventIDPrefix string = "audit-"  // Prefix for audit event IDs
	SessionIDPrefix    string = "sess-"   // Prefix for session IDs
	RequestIDPrefix    string = "req-"    // Prefix for request IDs
	UserRoleIDPrefix   string = "user-"   // Prefix for user role IDs
	AdminRoleIDPrefix  string = "admin-"  // Prefix for admin role IDs
	ClientIDPrefix     string = "client-" // Prefix for client IDs
)
